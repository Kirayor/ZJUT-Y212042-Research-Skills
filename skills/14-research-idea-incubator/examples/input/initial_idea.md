# Example Input Idea

Use diffusion models for adversarial example generation in visual-language model (VLM) security.

I am interested in whether diffusion-model image editing can create natural-looking and semantic-preserving image variants that make VLMs fail on benign captioning or VQA tasks. The project should avoid directly reproducing harmful jailbreaks and should focus on a lightweight, course-project-scale demo first.

## Expected Behavior

The skill should not immediately produce a final research question. It should first ask clarification questions, then create structured intermediate artifacts.
